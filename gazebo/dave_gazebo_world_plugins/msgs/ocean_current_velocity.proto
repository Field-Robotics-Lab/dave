syntax = "proto2";
package ocean_current_velocity_msgs.msgs;
import "vector3d.proto";
import "any.proto";

message StratifiedCurrentVelocity
{
  repeated gazebo.msgs.Vector3d   velocity  = 1;
  repeated gazebo.msgs.Any        depth     = 2;
}